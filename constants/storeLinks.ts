/**
 * Store listing URLs (Guide §4.1, asset #3).
 *
 * Play Store isn't live yet — `null` renders the badge in a disabled
 * "Coming soon" state rather than linking nowhere. Fill it in and the
 * badge becomes a real link with no other change.
 */
export const APP_STORE_URL =
  'https://apps.apple.com/us/app/yalbum-photo-albums/id6760424577'

export const PLAY_STORE_URL: string | null = null
